spring:
  datasource:
    # https://www.h2database.com/html/cheatSheet.html
    # Test 에서 In-Memory 환경으로 설정
    # 사실 스프링부트는 설정이 없으면 메모리 환경으로 뜰 수 있게 해준다
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        # System.out을 통해출력
#        #        show_sql: true
#        format_sql: true

logging:
  level:
    # logger를 통해 출력
    org.hibernate.SQL: debug
    # 쿼리 파라미터를 보는 설정
    org.hibernate.type: trace

